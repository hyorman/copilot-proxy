# Copilot Proxy CLI Configuration
# Copy this file to .env and fill in your values.

# GitHub token for Copilot SDK authentication (required)
# Get one via: gh auth token
GITHUB_TOKEN=

# Server port (default: 3000)
# PORT=3000

# Comma-separated bearer tokens for API auth (optional, auth disabled if empty)
# API_TOKENS=
